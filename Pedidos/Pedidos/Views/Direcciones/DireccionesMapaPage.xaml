<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mapa="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             x:Class="Pedidos.Views.Direcciones.DireccionesMapaPage">
    <ContentPage.Content>
        <StackLayout>
            <mapa:Map x:Name="mapa" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                <!--<mapa:Map.ItemTemplate>
                    <DataTemplate>
                        <mapa:Pin 
                            Type="Place" 
                            Position="{Binding Position}" 
                            Label="{Binding Name}" 
                            Address="{Binding Description}">
                        </mapa:Pin>
                    </DataTemplate> 
                </mapa:Map.ItemTemplate>-->
            </mapa:Map>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>